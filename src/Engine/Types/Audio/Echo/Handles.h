#pragma once

#include "Engine/Types/CommonTypes.h"
#include "Engine/Util/Handles.h"

namespace Echo
{
    DEFINE_OPAQUE_HANDLE(AudioSoundHandle)
    DEFINE_OPAQUE_HANDLE(AudioStreamHandle)
    DEFINE_OPAQUE_HANDLE(VirtualAudioHandle)
    DEFINE_OPAQUE_HANDLE(AudioBusHandle)
}